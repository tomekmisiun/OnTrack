name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kod
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Ustaw Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Uruchom testy FastAPI
        working-directory: backend
        run: |
          uv sync --dev
          uv run pytest tests/contract/ tests/integration/test_worker_catalog_seed.py \
            tests/test_health.py tests/test_dish_compare_data.py \
            -q --cov=app --cov-report=term-missing --cov-fail-under=50
        env:
          TESTING: "1"
          DATABASE_URL: "sqlite://"
          FLASK_SECRET_KEY: ci-test-flask-secret-key-0123456789abcdef
          JWT_SECRET_KEY: ci-test-jwt-secret-key-0123456789abcdef
          GOOGLE_CLIENT_ID: ci-test-google-client-id
          GOOGLE_CLIENT_SECRET: ci-test-google-client-secret

  frontend-next:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kod
        uses: actions/checkout@v6

      - name: Ustaw Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend-next/package-lock.json

      - name: Lint, typecheck i build frontend-next
        working-directory: frontend-next
        run: |
          npm ci
          npm run generate:api
          npm run test
          npm run lint
          npm run typecheck
          npm run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:5001

  frontend-next-e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kod
        uses: actions/checkout@v6

      - name: Ustaw Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend-next/package-lock.json

      - name: Playwright smoke tests
        working-directory: frontend-next
        run: |
          npm ci
          npm run generate:api
          npx playwright install --with-deps chromium
          npm run test:e2e
        env:
          CI: "true"
          NEXT_PUBLIC_API_URL: http://localhost:5001

  backend-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kod
        uses: actions/checkout@v6

      - name: Build FastAPI backend image
        run: docker build -t ontrack-fastapi backend

  frontend-next-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout kod
        uses: actions/checkout@v6

      - name: Build frontend-next production image
        run: |
          docker build -t ontrack-frontend-next \
            --target runner \
            --build-arg NEXT_PUBLIC_API_URL=http://localhost:5001 \
            -f frontend-next/Dockerfile frontend-next

  backend-integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: ontrack
          POSTGRES_PASSWORD: ontrack
          POSTGRES_DB: ontrack_rehearsal
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ontrack -d ontrack_rehearsal"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout kod
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"

      - name: Ustaw Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: DB rehearsal — stamp on legacy schema
        working-directory: backend
        run: |
          uv sync --dev
          uv run pytest tests/integration/ -v
        env:
          TEST_DATABASE_URL: postgresql+psycopg://ontrack:ontrack@localhost:5432/ontrack_rehearsal
          FLASK_SECRET_KEY: ci-test-flask-secret-key-0123456789abcdef
          JWT_SECRET_KEY: ci-test-jwt-secret-key-0123456789abcdef
