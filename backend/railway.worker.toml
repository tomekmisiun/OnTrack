# FastAPI staging worker — Root Directory: backend
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = [
  "app/**",
  "data/**",
  "scripts/**",
]

[deploy]
restartPolicyType = "on_failure"
startCommand = "sh scripts/start-worker.sh"
